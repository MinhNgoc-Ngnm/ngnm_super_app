import {StyleSheet} from 'react-native';
import gridStyles from './grids';

const dfStyle = StyleSheet.create({
  ...gridStyles,
});
export default dfStyle;

export const grid = StyleSheet.create(gridStyles);
